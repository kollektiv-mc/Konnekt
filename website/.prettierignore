# Generated by `pnpm gen:tokens` (frontend/scripts/gen-tokens.mjs). Its
# formatting is the generator's output: Prettier wants to rewrap the two long
# font stacks, the next run would revert that, and the result shows up as a
# dirty diff in the generated-file health check. Same reasoning as the token
# entries in frontend/.prettierignore.
tokens.css
